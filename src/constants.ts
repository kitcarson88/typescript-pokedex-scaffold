export const configuration = {
  SERVICE_TIMEOUT: 5, // Service timeout in seconds
  CACHE_TIMEOUT: 7 * 24 * 60, // Cache timeout in minutes
  CACHE_CLEAN_TIMEOUT: 7 * 24 * 60 + 5, // Cache timeout in minutes
  LOGGER_DATE_FORMAT: "DD-MM-YYYY HH:mm:ss Z",
};

export const endpoints = {
  POKEMON: "pokemon"
};